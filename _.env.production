# Rename this file to .env.production, change VITE_APP_ORIGIN to a domain you own and will run the app on, then delete this comment.
VITE_APP_ORIGIN="https://subdomain.mydomain.com"

# for testing the build locally
# VITE_APP_ORIGIN="http://localhost:3000"